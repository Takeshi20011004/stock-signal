# 株シグナル通知（楽天証券などで手動発注する前提）

実弾の自動売買は**しません**。買い/売りの「シグナル」を検出して macOS 通知＋ログに残すだけです。
発注の判断・操作はご自身で行ってください。

## 何を検出するか
- **移動平均クロス**: 25日線が75日線を上抜け＝ゴールデンクロス（買い）／下抜け＝デッドクロス（売り）
- **RSI**: 14日RSIが30を下抜け＝売られすぎ（買い候補）／70を上抜け＝買われすぎ（売り候補）
- **MACD**: MACD線がシグナル線を上抜け＝ゴールデンクロス（買い）／下抜け＝デッドクロス（売り）（既定 12/26/9）
- **ボリンジャーバンド**: 終値が +kσ を上抜け＝買われすぎ／-kσ を下抜け＝売られすぎ（既定 20日・k=2）
- **指定価格到達**: 「〇〇円以下／以上になったら通知」

データは Yahoo Finance から取得（無料・外部ライブラリ不要）。投資助言ではありません。

---

## 銘柄・価格の編集（あなたが触るのはここだけ）
`config.json` を編集します。

```json
"watchlist": [
  { "code": "7203", "name": "トヨタ" },     ← 監視したい銘柄を追加/削除
  { "code": "9984", "name": "ソフトバンクG" }
],
"price_alerts": [
  { "code": "7203", "below": 2500 },         ← 2500円以下で通知
  { "code": "9984", "above": 12000 }         ← 12000円以上で通知
]
```
- `code` は4桁の銘柄コード（`.T` は不要、自動で付きます）
- `price_alerts` は設定したい銘柄だけでOK。`below` と `above` は両方書いても可
- クロスとRSIは全 watchlist 銘柄に自動適用（価格指定不要）

---

## 手動で動かす
```bash
cd ~/stock-signal
python3 signal_check.py          # 通常実行（新規シグナルだけ通知）
python3 signal_check.py --test   # 重複防止を無視して必ず通知（動作確認用）
```
- `signals.log` … 通知の履歴
- `state.json` … 通知済みの記録（同じシグナルを二度通知しないため。消すと再通知され得る）

---

## 自動実行（平日15:40・スリープでも次回起動時に実行）
登録（有効化）:
```bash
cp ~/stock-signal/com.user.stock-signal.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.user.stock-signal.plist
```
停止（無効化）:
```bash
launchctl unload ~/Library/LaunchAgents/com.user.stock-signal.plist
```
今すぐ1回テスト実行:
```bash
launchctl start com.user.stock-signal
```
実行ログ: `launchd.log` / `run.log`

時刻を変えたい場合は plist の `Hour`/`Minute` を編集して unload→load し直してください。

---

## 通知が出ないとき
macOS「システム設定 → 通知」で **ターミナル（または Script Editor）** の通知が
許可されているか確認してください。初回は許可ダイアログが出ることがあります。

---

## 後で LINE 通知にする
`signal_check.py` の下部にある `notify_line()` の雛形を実装し、
`notify()` 内のコメントを外すだけで差し替えできます。
LINE Messaging API のチャネルアクセストークンと自分の userId が必要です（別途セットアップ）。
```

## 注意・限界
- Yahoo の無料データのため、株式分割・配当・遅延・銘柄コード違いで誤差が出ることがあります
- シグナルは「機械的な目安」であり、勝てる保証はありません。最終判断は必ずご自身で
- 監視銘柄を増やしすぎるとYahoo側に負荷をかけるので常識的な数に
